## Bug 1
Checkboxes lose formatting if they have anything else in them except for an x or space (e.g. `[-]`, `[?]`, `[N/A]`).

## Bug 2
Make lines with checked checkboxes a bit darker.

## Bug 3
Character misalignment in tables caused by nested markdown formatting and wide Unicode characters/emojis.

## Bug 4
Table column size auto-adjustment is incorrect on terminal resize or zoom, due to integer truncation in width calculation.

## Bug 5
Line numbers jump backward when rendering multi-line table rows or code blocks.
